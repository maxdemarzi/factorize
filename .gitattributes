# Windows checkouts were converting shell scripts to CRLF, which makes bash fail
# on the shebang line ("set: pipefail: invalid option name") when the repo is
# built from WSL -- the documented build path (DECISIONS D7). Normalise on LF.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary
*.zst binary
